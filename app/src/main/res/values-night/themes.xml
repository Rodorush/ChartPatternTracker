<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal do aplicativo -->
    <style name="Theme.ChartPatternTracker" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Cor de fundo principal do aplicativo -->
        <item name="android:colorBackground">@color/textColor</item>
        <!-- Cor do texto principal -->
        <item name="android:textColorPrimary">@color/backgroundColor</item>
        <!-- Cor primária (para botões e elementos principais) -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryVariant</item>
        <!-- Cor de destaque (para elementos secundários) -->
        <item name="colorAccent">@color/secondaryColor</item>
    </style>
</resources>